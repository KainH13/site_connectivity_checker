import React from "react";

// components
import ConnectionCheckForm from "../components/ConnectionCheckForm";

const Home = (props) => {
  return (
    <ConnectionCheckForm />
  );
};

export default Home;
